---
import { cn } from "@/lib/utils/cn";
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"thead"> & {};

const { class: className, ...attr } = Astro.props;
---

<thead class={cn("[&_tr]:border-b", className)} {...attr}>
  <slot />
</thead>
