---
import { cn } from "@/lib/utils/cn";
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"tbody"> & {};

const { class: className, ...attr } = Astro.props;
---

<tbody class={cn("[&_tr:last-child]:border-0", className)} {...attr}>
  <slot />
</tbody>
