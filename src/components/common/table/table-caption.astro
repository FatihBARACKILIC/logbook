---
import { cn } from "@/lib/utils/cn";
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"caption"> & {};

const { class: className, ...attr } = Astro.props;
---

<caption class={cn("mt-4 text-sm text-muted-foreground", className)} {...attr}>
  <slot />
</caption>
