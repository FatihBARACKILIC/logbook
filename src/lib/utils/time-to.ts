/**
 * The function `secondToMilliseconds` converts seconds to milliseconds by multiplying the input value
 * by 1000.
 * @param {number} seconds - The `seconds` parameter is a number representing the time duration in
 * seconds that you want to convert to milliseconds.
 */
export const secondToMilliseconds = (seconds: number) => seconds * 1000;
